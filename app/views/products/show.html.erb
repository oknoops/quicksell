<div class="container">
  <div class="row">
    <div class="col-sm-12 no-gutters-2">
      <div class = "show-product-images" style = "background-image: url(<%= cl_image_path @product.picture %>);"></div>
    </div>
  </div>
</div>
<div class="container product-information">
  <div class="row">
    <div class="col-xs-12 col-sm-4">
      <div class = 'product-show-details'>
        <i class="fas fa-money-bill-alt fa-3x"></i>
        <p><%= @product.price %> €</p>
      </div>
    </div>
    <div class="col-xs-12 col-sm-4">
      <div class = 'product-show-details'>
        <i class="fas fa-map-marker-alt fa-3x"></i>
        <p><%= @product.pickup_address %></p>
      </div>
    </div>
    <div class="col-xs-12 col-sm-4">
      <div class = 'product-show-details'>
        <div class = 'logo-circle-effacer'>
          <i class="fas fa-clock fa-3x"></i>
        </div>
        <p><%= @product.pickup_time %></p>
      </div>
    </div>

    <div class = 'row'>
      <div class = 'col-xs-12 col-sm-12 center'>
        <% if @product.status == "for-sale" && @product.user != current_user %>
          <%= form_tag product_sales_path(@product, @sales) do %>
            <%= hidden_field_tag 'product_id', @product.id, method: :post %>
            <%= submit_tag 'Purchase', class: 'button-alternative' %>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h2>Description</h2>
      <%= @product.description %>
    </div>
  </div>
</div>

<% if @product.user == current_user %>
  <div class="container product-information">
    <div class="row">
      <div class="col-sm-12">
        <h2>Options</h2>
        <div class = 'col-sm-6 center'>
          <%= link_to "Delete", product_path(@product), class: 'button-alternative', method: :delete, data: { confirm: "Are you sure you want to delete this product?" } %>
        </div>
        <div class = 'col-sm-6 center'>
          <%= link_to "Edit",edit_product_path(@product), class: 'button-alternative' %>
        </div>
      </div>
    </div>
  </div>
<% end %>

